{
  "name": "lab1-calculator",
  "version": "1.0.0",
  "description": "Calculator with comprehensive testing",
  "private": true,
  "scripts": {
    "test": "npm run test:backend && npm run test:frontend && npm run test:e2e",
    "test:backend": "cd backend && composer test",
    "test:frontend": "cd frontend && npm test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "install:all": "cd backend && composer install && cd ../frontend && npm install && cd .. && npm install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1"
  }
}

